// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    // Habilitar styled-components
    styledComponents: true,
  },
};

export default nextConfig;
